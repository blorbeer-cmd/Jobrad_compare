{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -q 'git push'; then cd /home/user/Jobrad_compare && npx eslint src/ --max-warnings 0; fi",
            "timeout": 60,
            "statusMessage": "ESLint vor git push..."
          }
        ]
      }
    ]
  }
}
